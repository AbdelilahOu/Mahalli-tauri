import { defineComponent } from "vue";

export const ClientCredi = defineComponent({
  name: "ClientCredi",
  setup(props) {
    return () => (
      <div>
        <h1>Credi</h1>
      </div>
    );
  },
});
